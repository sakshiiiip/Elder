<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet required for Gemini API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Microphone for speech recognition -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Required for drawing over other apps (floating overlay & highlight overlay) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Required for foreground service that keeps overlay alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Declare package visibility queries for Speech Recognition Service (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_sahaay_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sahaay_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElderHelpPrototypeV01">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ElderHelpPrototypeV01"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sahaay Floating Overlay Service -->
        <service
            android:name=".overlay.SahaayOverlayService"
            android:exported="false"
            android:stopWithTask="true"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="assistive_technology" />
        </service>

        <!-- Sahaay Accessibility Service for Screen Inspection & Highlighting -->
        <service
            android:name=".accessibility.SahaayAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="Sahaay Accessibility Assistant">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>